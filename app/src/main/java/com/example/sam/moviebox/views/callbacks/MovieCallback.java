package com.example.sam.moviebox.views.callbacks;

import android.widget.ImageView;

import com.example.sam.moviebox.model.MovieModel;

public interface MovieCallback {
    void onClick(MovieModel movieModel);
}
